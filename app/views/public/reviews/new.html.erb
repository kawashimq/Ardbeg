<h1>画像投稿フォーム</h1>
  <%= form_for @review, url: public_reviews_path do |f| %>
    <h4>画像投稿</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>商品名</h4>
      <%= f.text_field :item %>
    <h4>口コミ</h4>
      <%= f.text_area :content %>

    <%= f.submit '投稿' %>
  <% end %>